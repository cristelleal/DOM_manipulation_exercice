function createTitle(title) {
  const titleItem = document.createElement("li");
  istItem.textContent = `${title[0]} ${title[1]}`;
  listItem.style.listStyle = "none";
  listItem.style.textAlign = "left";

  return titleItem;
}
